version: '3.8'

services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: myunsejeomju_db
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_USER: myunsejeomju_user
      MYSQL_PASSWORD: myunsejeomju_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    networks:
      - myunsejeomju_network

  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=django-insecure-docker-secret-key-change-in-production
      - DEBUG=True
      - ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
      - DB_NAME=myunsejeomju_db
      - DB_USER=myunsejeomju_user
      - DB_PASSWORD=myunsejeomju_password
      - DB_HOST=db
      - DB_PORT=3306
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
    networks:
      - myunsejeomju_network
    command: >
      sh -c "uv run python manage.py migrate &&
             uv run gunicorn --config gunicorn.conf.py myunsejeomju.wsgi:application"

volumes:
  mysql_data:

networks:
  myunsejeomju_network:
    driver: bridge